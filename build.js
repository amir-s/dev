#!/usr/bin/env node
// import { resolve } from "path";
// import { esbuildPluginFilePathExtensions } from "esbuild-plugin-file-path-extensions";
// import esbuild from "esbuild";

// esbuild
//   .build({
//     format: "js",
//     entryPoints: ["./main.ts", "./modules/**/*.ts"],
//     outdir: "lib", // Output directory
//     platform: "node", // Platform for Node.js
//     target: "es2022", // Target version
//     // sourcemap: true, // Optional: Generate sourcemaps
//     tsconfig: "tsconfig.json", // Optional: Use tsconfig.json
//     plugins: [esbuildPluginFilePathExtensions()],
//   })
//   .catch(() => process.exit(1));
